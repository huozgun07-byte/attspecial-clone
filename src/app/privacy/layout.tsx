// Metadata for /privacy lives in the page itself (it is a server component).
// This layout is kept only as a pass-through.
export default function PrivacyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
